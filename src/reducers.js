const initialState = {
  access_token: '',
  flags: {
    token: null
  }
};

const convertyApp = (state = initialState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default convertyApp;
